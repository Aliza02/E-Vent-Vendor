import 'package:flutter/material.dart';

class All_orders extends StatelessWidget {
  const All_orders({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('All Orders');
  }
}
